import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samir.mergebattle',
  appName: 'Merge Battle',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
